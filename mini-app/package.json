{
  "name": "vpn-mini-app",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
